#include <stdio.h>

int main()

{

int a, b, c;
float result;
printf("Enter the value of a in ax^2 + bx +c=0\n");
scanf("%d", &a);
printf("Enter the value of b in ax^2 + bx +c=0\n");
scanf("%d", &b);
printf("Enter the value of a in ax^2 + bx +c=0\n")
scanf("%d", &c);

result = ax^2 + bx + c = 0;
printf("The results is:%f", result);

}
