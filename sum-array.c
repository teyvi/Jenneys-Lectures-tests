#include <stdio.h>
int main ()
{

int a [5], b[5], i;
int sumarray[i];

printf("Enter five numbers:");
for (i = 0; i <5; i++)
	scanf("%d", &a[i]);

printf("Enter five numbers:");
for(i = 0; i <5; i++)
	scanf("%d", &a[i]);

for(i =0; i <5; i++)
{

	sumarray[i] = a[i] + b[i];
	printf("\nThe sum of %d is %d:",i, sumarray[i]);
}

}
